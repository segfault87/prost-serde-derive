syntax = "proto3";

package repeated;

message Repeated {
    int32 id = 1;
    int64 timestamp = 2;
    repeated string names = 3;
    repeated bytes hashed_password = 4;
    optional bool is_active = 5;
}
